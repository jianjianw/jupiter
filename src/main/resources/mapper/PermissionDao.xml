<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiein.jupiter.web.dao.PermissionDao">
    <resultMap id="baseMap" type="com.qiein.jupiter.web.entity.po.PermissionPO">
        <id column="ID" property="permissionId" jdbcType="BIGINT"/>
        <result column="PERMISSIONNAME" property="permissionName" jdbcType="VARCHAR"/>
        <result column="ABBREVIATE" property="abbreviate" jdbcType="VARCHAR"/>
        <result column="TYPEID" property="typeId" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="baseColumn">
        ID,PERMISSIONNAME,TYPEID,ABBREVIATE
    </sql>

    <sql id="tableName">
        hm_pub_permission
    </sql>

    <select id="getById">
        SELECT
        <include refid="baseColumn"/>
        FROM
        <include refid="tableName"/>
        WHERE ID = #{id}
    </select>

    <select id="findList">
        SELECT
        <include refid="baseColumn"/>
        FROM
        <include refid="tableName"/>
    </select>

    <insert id="insert">
        <include refid="tableName"/>
        (ID,PERMISSIONNAME,ABBREVIATE,TYPEID)
        VALUES (
        #{id}, #{permissionName},#{abbreviate}, #{typeId}
        )
    </insert>

    <update id="update">
        UPDATE
        <include refid="tableName"/>
        <set>
            <if test="permissionName != null">
                PERMISSIONNAME = #{permissionName},
            </if>
            <if test="abbreviate != null">
                abbreviate = #{abbreviate},
            </if>
            <if test="typeId != null">
                TYPEID = #{typeId},
            </if>
        </set>
        WHERE ID = #{id}
    </update>

</mapper>