<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiein.jupiter.web.dao.FastMemoDao">

    <!--根据员工ID，获取快捷备注列表-->
   <select id="getMemoListById" parameterType="map" resultType="java.lang.String">
       SELECT MEMO
       FROM hm_crm_fast_memo
       WHERE STAFFID = #{staffId}
       AND COMPANYID = #{companyId};
   </select>

    <!--添加快捷备注-->
    <insert id="addFastMemo" parameterType="map">
        INSERT INTO (MEMO, STAFFID, COMPANYID )
        VALUES (#{memo}, #{staffId}, #{companyId})
    </insert>
</mapper>