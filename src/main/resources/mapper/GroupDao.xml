<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiein.jupiter.web.dao.GroupDao">
    <resultMap id="baseMap" type="com.qiein.jupiter.web.entity.po.GroupPO">
        <id column="ID" property="id" jdbcType="BIGINT"/>
        <result column="GROUPID" property="groupId" jdbcType="VARCHAR"/>
        <result column="GROUPNAME" property="groupName" jdbcType="VARCHAR"/>
        <result column="PARENTID" property="parentId" jdbcType="VARCHAR"/>
        <result column="PRIORITY" property="priority" jdbcType="VARCHAR"/>
        <result column="GROUPTYPE" property="groupType" jdbcType="VARCHAR"/>
        <result column="COMPANYID" property="companyId" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="baseColumn">
        ID, GROUPID, GROUPNAME, PARENTID, PRIORITY, GROUPTYPE, COMPANYID
    </sql>

    <sql id="tableName">
        hm_pub_group
    </sql>

    <select id="getById" parameterType="java.lang.Integer" resultMap="baseMap">
        SELECT
        <include refid="baseColumn"/>
        FROM <include refid="tableName"/>
        WHERE ID = #{id}
    </select>

    <insert id="insert" parameterType="com.qiein.jupiter.web.entity.po.GroupPO"  useGeneratedKeys="true" keyProperty="id">
        INSERT INTO <include refid="tableName"/>
        {GROUPID, GROUPNAME, PARENTID, PRIORITY, GROUPTYPE, COMPANYID}
        VALUES (#{groupId}, #{groupName}, #{parentId}, #{priority}, #{groupType}, #{companyId})
    </insert>


</mapper>